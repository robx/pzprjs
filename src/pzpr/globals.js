import Candle from 'pzpr-canvas';

var document      = this.document      || Candle.document;
var DOMParser     = this.DOMParser     || Candle.DOMParser;
var XMLSerializer = this.XMLSerializer || Candle.XMLSerializer;


export {document, DOMParser, XMLSerializer};
