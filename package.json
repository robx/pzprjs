{
  "name": "pzpr",
  "version": "0.0.2",
  "description": "Scripts for handling puzzle objects",
  "main": "./index.js",
  "directories": {
    "test": "test"
  },
  "keywords": [
    "puzzle",
    "HTML5"
  ],
  "author": "sabo2",
  "license": "MIT",
  "repository": {
    "type": "hg",
    "url": "https://bitbucket.org/sabo2/pzprjs"
  },
  "homepage": "https://bitbucket.org/sabo2/pzprjs",
  "engines": {
    "node": ">= 4.0.0"
  },
  "scripts": {
    "build": "grunt default",
    "release": "grunt release",
    "release:gzip": "sh -c 'cd dist; tar czf $0 --exclude *.concat.js --exclude \"*.map\" --exclude .DS_Store *; mv $0 ..' pzpr-$npm_package_version.tar.gz",
    "release:bz2": "sh -c 'cd dist; tar cjf $0 --exclude *.concat.js --exclude \"*.map\" --exclude .DS_Store *; mv $0 ..' pzpr-$npm_package_version.tar.bz2",
    "release:zip": "sh -c 'cd dist; zip -9qr $0 * -x *.concat.js -x *.map -x pzpr-variety/*.map -x .DS_Store; mv $0 ..' pzpr-$npm_package_version.zip",
    "clean": "grunt clean",
    "lint": "grunt lint",
    "test": "grunt lint && mocha -R progress",
    "prepublish": "npm run release"
  },
  "dependencies": {
    "jsdom": "^7.2.2"
  },
  "devDependencies": {
    "grunt": "^0.4.5",
    "grunt-contrib-clean": "^0.7.0",
    "grunt-contrib-concat": "^0.5.1",
    "grunt-contrib-copy": "^0.8.2",
    "grunt-contrib-jshint": "^0.11.3",
    "grunt-contrib-uglify": "^0.11.0",
    "grunt-newer": "^1.1.1"
  }
}
